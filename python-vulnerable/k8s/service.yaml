apiVersion: v1
kind: Service
metadata:
  name: python-vulnerable-service
  namespace: python-vulnerable
spec:
  selector:
    app: python-vulnerable
  ports:
  - port: 5001
    targetPort: 5001
  type: NodePort
